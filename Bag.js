class Bag {
    constructor(weight) {
        if (!weight) {
          throw new Error("bag needs weight");
        }
        this.weight = weight;
      }

    // Include constructor Method with the following property:  
    
}

//Export the module
module.exports = Bag;